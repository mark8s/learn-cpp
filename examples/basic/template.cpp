// 模板
// 人们需要编写多个形式和功能都相似的函数，因此有了函数模板来减少重复劳动；
// 人们也需要编写多个形式和功能都相似的类，于是 C++ 引人了类模板的概念，编译器从类模板可以自动生成多个类，避免了程序员的重复劳动。
/**
 *
 * 函数模板的定义
 template <typename type> ret-type func-name(parameter list){
   // 函数的主体
 }

 * 类模板的定义
  template <class type> class class-name {
     ...
  }
 *
 */

